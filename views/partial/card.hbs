<div class="col-4">
<div class="card" style="width: 18rem;">
        <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
        <div class="card-body">
            <h5 class="card-title">{{this.name}}</h5> <i class="bi-heart-fill"></i>
            <i class="bi-heart"></i>
            
            Ingredients:
            <ul>
                {{#each this.ingredients}}
                <li>{{amount}}{{unit}} {{name}}</li>
                {{/each}}
            </ul>
            <p class="card-text">Method: <br> {{this.method}}</p>
            <p class="card-text">Garnish: <br> {{this.garnish}}</p>
            <p class="card-text">Note: <br> {{this.note}}</p>

            <a href="#" class="btn btn-dark">Edit</a>
            <span class="badge rounded-pill bg-primary">{{this.source}}</span>
            <br>
            <span class="badge bg-secondary">{{this.ingredients.0.name}}</span>
    
            
        </div>
    </div>
    </div>

    {{!-- <div class="card mb-3" style="max-width: 540px;"> 
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{this.imageUrl}}" class="img-fluid rounded-start" alt="{{this.name}}">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h4 class="card-title">{{this.name}}</h4>
        <h5 class="card-subtitle">Ingredients:</h5>
        <ul>
                {{#each this.ingredients}}
                <li>{{amount}}{{unit}} {{name}}</li>
                {{/each}}
            </ul>
        <h5 class="card-subtitle">Method:</h5>
        <p class="card-text">{{this.method}}</p>
        
        <p class="card-text">{{this.garnish}}</p>
        <small class="text-muted">Last updated 3 mins ago</small></p>
      </div>
    </div>
  </div>
</div> --}}